import React from 'react';

const MyComment = () => {
    return (
        <div>
            <h2>My Reviews</h2>
            {/* Các đánh giá của người dùng sẽ được hiển thị tại đây */}
            <p>Danh sách các đánh giá của bạn về sản phẩm.</p>
            {/* Bạn có thể thêm code để lấy và hiển thị đánh giá từ API hoặc state */}
        </div>
    );
};

export default MyComment;
